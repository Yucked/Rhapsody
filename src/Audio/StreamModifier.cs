using System.IO;

namespace Frostbyte.Audio
{
    public sealed class StreamModifier
    {
        public static AudioStream ToPcm16(Stream stream)
        {
            return default;
        }
    }
}